# ============================================
# WinScan Environment Variables
# ============================================
# ⚠️ IMPORTANT: All variables below are OPTIONAL!
# The app works out-of-the-box using RPC/API from Chains/*.json files
# Only configure these if you have a custom backend or want to override defaults

# ============================================
# SSL Backend Configuration (OPTIONAL - WinSnip only)
# ============================================
# These are WinSnip's custom SSL backend endpoints
# If you're forking this project:
# - Leave these EMPTY to use RPC/API from chain config files directly ✅
# - Or set your own backend URLs if you have one
API_URL=
API_URL_FALLBACK=

# ============================================
# Public API Configuration (OPTIONAL)
# ============================================
# Frontend will use these URLs for API calls
# Leave empty to use chain config files
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_API_URL_FALLBACK=
NEXT_PUBLIC_BACKEND_URL=
NEXT_PUBLIC_WINSCAN_URL=

# ============================================
# API Timeouts & Performance
# ============================================
NEXT_PUBLIC_API_TIMEOUT=8000
NEXT_PUBLIC_CACHE_TTL=30000

# ============================================
# Polling Intervals (milliseconds)
# ============================================
# How often to refresh data on each page
NEXT_PUBLIC_POLL_VALIDATOR_DETAIL=30000
NEXT_PUBLIC_POLL_VALIDATORS=30000
NEXT_PUBLIC_POLL_OVERVIEW=20000
NEXT_PUBLIC_POLL_BLOCKS=15000
NEXT_PUBLIC_POLL_TRANSACTIONS=15000
NEXT_PUBLIC_POLL_UPTIME=30000

# ============================================
# Homepage Configuration
# ============================================
# Set to 1 to disable homepage and redirect to default chain
# NEXT_PUBLIC_DISABLE_HOMEPAGE=0
NEXT_PUBLIC_DEFAULT_CHAIN=paxi-mainnet

# ============================================
# Domain Whitelist (Optional)
# ============================================
# Comma-separated list of allowed domains
# Leave empty to allow all domains
# Example: winscan.org,winscan.winsnip.xyz,localhost
NEXT_PUBLIC_ALLOWED_DOMAINS=

# ============================================
# IMPORTANT NOTES FOR FORKERS:
# ============================================
# ✅ NO ENVIRONMENT VARIABLES REQUIRED!
#    The app works perfectly without any .env file
#    All RPC/API endpoints are in Chains/*.json files
#
# 1. Quick Start (No env needed):
#    - Fork the repo
#    - Deploy to Vercel (no env vars needed)
#    - It just works! ✨
#
# 2. Optional: Custom Backend
#    If you have your own backend API:
#    - Set API_URL and NEXT_PUBLIC_API_URL above
#    - Otherwise leave empty
#
# 3. For Vercel deployment:
#    - No environment variables required
#    - Optional: Add NEXT_PUBLIC_DEFAULT_CHAIN if you want
#    - That's it!
#
# 4. For local development:
#    - Just run: npm run dev
#    - No .env file needed
#    - Optional: Copy this file to .env if you want to customize
